#version 430

// Model/view/projection matrix
layout(location = 0) in vec3 pos; // World-space position
varying vec2 textureCoord;

const vec2 madd=vec2(0.5,0.5);

void main(void) {
    textureCoord = pos.xy*madd+madd;
    gl_Position = vec4(pos,1.0);
}